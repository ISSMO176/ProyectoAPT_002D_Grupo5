{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "setup": "npm run env-setup && npm install && npx prisma migrate deploy && npx prisma generate && node src/app.js",
    "env-setup": "cp .env.example .env || echo '.env ya existe'",
    "start": "npm run setup && node src/app.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "backend": "file:",
    "bcrypt": "^5.1.1",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "jsonwebtoken": "^9.0.2",
    "nodemon": "^3.1.7",
    "pg": "^8.13.0",
    "pg-hstore": "^2.3.4",
    "prisma": "^5.22.0",
    "proyecto": "file:..",
    "sequelize": "^6.37.4"
  },
  "description": "",
  "proxy": "http://localhost:5000/"
}
